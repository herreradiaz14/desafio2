import { DirectivaGeneralDirective } from './directiva-general.directive';

describe('DirectivaGeneralDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaGeneralDirective();
    expect(directive).toBeTruthy();
  });
});
